import {
  upperCase
} from 'marvel-world-cli/helpers/upper-case';

module('UpperCaseHelper');

// Replace this with your real tests.
test('it works', function() {
  var result = upperCase(42);
  ok(result);
});
