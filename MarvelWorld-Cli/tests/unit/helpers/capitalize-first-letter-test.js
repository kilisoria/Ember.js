import {
  capitalizeFirstLetter
} from 'marvel-world-cli/helpers/capitalize-first-letter';

module('CapitalizeFirstLetterHelper');

// Replace this with your real tests.
test('it works', function() {
  var result = capitalizeFirstLetter(42);
  ok(result);
});
