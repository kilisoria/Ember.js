import {
  moduleFor,
  test
} from 'ember-qunit';

moduleFor('controller:movie/index', 'MovieIndexController', {
  // Specify the other units that are required for this test.
  // needs: ['controller:foo']
});
