import randomNumber from 'marvel-world-cli/utils/random-number';

module('randomNumber');

// Replace this with your real tests.
test('it works', function() {
  var result = randomNumber();
  ok(result);
});
