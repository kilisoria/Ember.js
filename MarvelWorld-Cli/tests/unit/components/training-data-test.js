import {
  moduleForComponent,
  test
} from 'ember-qunit';

moduleForComponent('training-data', 'TrainingDataComponent', {
  // specify the other units that are required for this test
  // needs: ['component:foo', 'helper:bar']
});


